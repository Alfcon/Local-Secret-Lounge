python3 - <<'PY'
import json
from pathlib import Path

base = Path.home() / "The_App/data/discover_characters"

for f in sorted(base.glob("*/*.json")):
    try:
        json.loads(f.read_text(encoding="utf-8"))
        print("OK   ", f)
    except Exception as e:
        print("FAIL ", f, "-", e)
PY


